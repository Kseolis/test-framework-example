# Test review report — <PR / branch>

> Generated by `test-code-reviewer` on <date>. Do not edit by hand.

## Summary

| Severity | Count |
| -------- | ----- |
| Blocker  | 0     |
| Major    | 0     |
| Minor    | 0     |

Verdict: ✅ approve / ⚠️ request-changes / ❌ block.

## Findings

| File:Line                    | Severity | Rule         | Anti-pattern                | Suggested fix                                |
| ---------------------------- | -------- | ------------ | --------------------------- | -------------------------------------------- |
| tests/specs/login.spec.ts:42 | blocker  | no-hard-wait | `page.waitForTimeout(2000)` | Replace with `expect(locator).toBeVisible()` |

## Coverage observations

- AC-3 (locked-account rejection) referenced in design but no test tagged `@ac:AC-3`.
- New endpoint `POST /payments/refunds` has client method but no API spec yet.

## Architectural observations

- `tests/pages/CheckoutPage.ts` is 412 LOC. Consider extracting `PaymentSection` and `ShippingSection` components.
- `tests/fixtures/index.ts` directly extends 7 fixtures inline. Consider `mergeTests`.

## Recommendations

1. Address blockers before merge.
2. Open a follow-up issue for the architectural observations.
3. Add `@ac:AC-3` tag to the relevant test or write the missing scenario.
